build/
.gradle/
local.properties
.kotlin/

# Resolved from Maven Central by .github/scripts/maven-publish-plan.sh at release time and read by
# the Gradle build; never committed, because Central is the source of truth for what is published.
publishing-manifest.json
